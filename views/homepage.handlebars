  <body onload="reloadUserList()">
  <div>
    <h2>Current user: <span id="current-user">{{username}}</span>
      </h2>
  </div>
<div>
  {{>calendar }}
  <div> 
      {{#each events as |events|}}
      {{> events-details}}
      {{/each}}
  </div>
</div>
  {{> chat-box}}
 </body>