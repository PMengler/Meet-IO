<div>
  {{>calendar }}
  <div> 
      {{#each events as |events|}}
      {{> events-details}}
      {{/each}}
  </div>
  <div>
    <h2>{{{online_status}}} {{username}}</h2>
  </div>
</div>
  {{> chat-box}}
